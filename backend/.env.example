PORT=5000
MONGODB_URI=mongodb://localhost:27017/lehra-companion
JWT_SECRET=change-me
CLIENT_ORIGIN=http://localhost:5173
# Uncomment if Node can't resolve MONGODB_URI's hostname even though your
# browser/OS can (common with VPNs or Cloudflare WARP pointing Node's DNS
# at a loopback proxy that refuses Node's own lookups).
# DNS_SERVERS=1.1.1.1,8.8.8.8
