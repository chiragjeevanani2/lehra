# Absolute URL of the deployed backend's API, e.g. https://lehra-api.onrender.com/api
# Leave unset for local dev — Vite proxies relative /api calls to localhost:5000 instead (vite.config.js).
VITE_API_BASE_URL=
